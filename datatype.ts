// Type Annotation
let age: number = 21;
let name: string = "Lekhaa";

// Type Inference
let city = "Hyderabad"; // inferred as string

// Tuple
let person: [string, number] = ["Anshu", 22];

// Enum
enum Direction { Up, Down, Left, Right }
let move: Direction = Direction.Left;

// Union
let id: string | number = 101;
